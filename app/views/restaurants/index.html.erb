<h1>All Restaurants</h1>
<% @restaurants.each do |restaurant| %>
<div><%= link_to restaurant.name, restaurant_path(restaurant) %><%= link_to "Add a new review", new_restaurant_review_path(restaurant), class:"bi bi-bookmark-star-fill" %></div>

<% end %>
<div><%= link_to "Add a new restaurant", new_restaurant_path, class:"btn btn-primary" %></div>
